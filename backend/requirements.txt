# This file mirrors the runtime dependencies in pyproject.toml for deployment
# platforms that install from requirements.txt.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.1

# Constrained model and read-only evidence provider adapters
openai>=2.8.0,<4
tavily-python>=0.5.0
