[phases.setup]
nixPkgs = ["nodejs", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "cd .. && pip install -e ."
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
