[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pip install -e . && pip install -r flask_frontend/requirements.txt && cd flask_frontend && gunicorn -k gevent -w 1 --timeout 0 -b 0.0.0.0:$PORT app:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
